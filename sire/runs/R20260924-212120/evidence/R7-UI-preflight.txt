CUA environment preflight before R7: `getState()` returned `apps=[]` and only an empty Codex in-app browser; native desktop app automation is disabled in the current tool surface. Attempt to enumerate native apps was unavailable (`cua.listApps is not a function`). Therefore Codex and Claude UI user paths cannot be opened or driven here. UI tests remain BLOCKED-无法真实验证; file system aliases and actual CLI paths are tested separately.
