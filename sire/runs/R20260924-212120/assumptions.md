# 假设与边界

- 目标是将 SIRE 内容归档到 `@SIRE_ROOT@\AI\AI` 当前 Git 仓库的独立 `sire/` 子目录；保留 `feishu-hub/` 原样。
- 用户已明确选择目标仓库并要求相关数据库纳入 Git；允许在当前本地分支做新增提交，不推送到 GitHub。
- 为避免把本机身份和环境路径写入 Git，数据库快照可保留全部业务记录与表结构，但需规范化本机绝对路径、邮箱样式字段和私网 IP；不修改运行库原文件。
- Git 归档与 SIRE 轻量 ZIP 分发是两种独立交付：本归档按用户要求含数据库快照；现有 ZIP 打包策略仍排除数据库和历史运行产物。
- 明确排除密钥/加密 vault、工具个人配置快照、模型权重、SQLite sidecar、缓存和备份副本。
