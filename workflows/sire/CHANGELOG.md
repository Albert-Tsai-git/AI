# SIRE 工作流变更记录

> 本机 `SIRE-MIGRATION.md` 需同步以下变更（SKILL 规定：修改规则须同步迁移文档）。

## v6.1（2026-09-24）

| 变更 | 原规则 | 新规则 | 理由 | 回滚 |
|---|---|---|---|---|
| 触发范围 | 所有用户消息加载 | 仅开发类任务加载，`sire` 前缀可强制 | 与 description 矛盾；问答场景开销大 | 恢复原句 |
| 通道分级 | 仅“一行修改”可走轻量 | 轻量 / 标准 / 完整三档，只升不降 | 小任务成本过高 | 删除“通道分级”节 |
| 打包 | 历史 zip 必须打入 | 历史 zip 仅记哈希入清单 | 包体逐次嵌套膨胀 | 恢复原句 |
| 路径 | 硬编码 `%USERPROFILE%`、`.claude`/`.codex` 混用 | `SIRE_HOME`/`SIRE_SCRIPTS`/`SIRE_DB` | 可移植；脚本统一一处 | 替换回绝对路径 |
| 向量库文档 | 描述 v1 哈希 384 维 | 描述 v2 混合检索，保留 v1 回退说明 | 与 F-028 实现不一致 | 恢复原节 |

验证：结构与链接人工检查；需在本机跑一个代表性回归任务后标记 active（按 self-optimization §4）。
